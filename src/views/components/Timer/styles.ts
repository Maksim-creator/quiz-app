import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  timerContainer: {
    height: '50%',
    justifyContent: 'center',
  },
  timer: {
    alignSelf: 'center',
    borderWidth: 3,
    borderColor: 'black',
    borderRadius: 100,
    height: 100,
    width: 100,
    justifyContent: 'center',
    alignItems: 'center',
  },
  timerText: {
    fontSize: 25,
  },
});
